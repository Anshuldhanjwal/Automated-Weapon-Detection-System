# AWDEFS — Python Dependencies
# Install with: pip install -r requirements.txt

# Web UI
streamlit>=1.28.0

# Deep Learning & Detection
ultralytics>=8.0.0

# PyTorch CPU-only (avoids downloading the ~2 GB CUDA variant)
# Streamlit Cloud is CPU-only, so this is correct for deployment.
torch>=2.0.0
torchvision>=0.15.0

# Image Processing
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Utilities
PyYAML>=6.0

huggingface_hub>=0.20.0
